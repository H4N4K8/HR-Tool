@model IEnumerable<HR_Tool.Models.Competency>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CompetencyName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CompetencyName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CompetencyID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CompetencyID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CompetencyID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
